<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>
      {% if page.title is defined %}
        {{ page.title }} · DonorDocs
      {% else %}
        DonorDocs
      {% endif %}
    </title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Work+Sans:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <style>
      :root {
        color-scheme: dark;
      }
      body {
        font-family: 'Work Sans', system-ui, sans-serif;
        background: #0e0e0e;
        color: #f5f5f5;
      }
      a {
        color: #61dafb;
      }
      .brand-accent {
        color: #61dafb;
      }
      .card {
        background-color: #1a1a1a;
        border: 1px solid #2b2b2b;
      }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg sticky-top border-bottom border-dark" style="background-color: #0e0e0e;">
      <div class="container-fluid">
        <a class="navbar-brand text-light fw-semibold brand-accent" href="{{ url_for('dashboard') }}">
          DonorDocs
        </a>
        <div class="d-flex align-items-center gap-2">
          <button class="btn btn-outline-info btn-sm" type="button" data-action="toggle-theme">
            Toggle theme
          </button>
        </div>
      </div>
    </nav>
    <main class="container py-4">
      {% block content %}{% endblock %}
    </main>
    <script type="module" src="{{ app.url }}/assets/js/app.js"></script>
  </body>
</html>
