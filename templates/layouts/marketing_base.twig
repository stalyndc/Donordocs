<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1"
    />
    <title>
      {% if page.title is defined %}
        {{ page.title }} · DonorDocs
      {% else %}
        DonorDocs — IRS-Compliant Donation Receipts
      {% endif %}
    </title>
    <meta
      name="description"
      content="{{ page.description|default('DonorDocs helps U.S. nonprofits generate IRS-compliant donation receipts, PDFs, and Excel reports.') }}"
    />
    <meta property="og:title" content="{{ page.title|default('DonorDocs — IRS-Compliant Donation Receipts') }}" />
    <meta
      property="og:description"
      content="{{ page.description|default('Generate branded, audit-ready receipts and Excel reports in seconds.') }}"
    />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="{{ app.url }}/" />
    <meta property="og:image" content="{{ app.url }}/assets/og-image.png" />
    <link rel="canonical" href="{{ app.url }}/" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Work+Sans:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="{{ app.url }}/assets/css/marketing.css" />
  </head>
  <body class="marketing-body">
    <nav class="navbar navbar-expand-lg navbar-dark py-3 marketing-navbar" aria-label="Main navigation">
      <div class="container">
        <a class="navbar-brand fw-semibold text-light" href="{{ url_for('marketing.home') }}">
          DonorDocs
        </a>
        <button
          class="navbar-toggler text-light border-0"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#marketingNav"
          aria-controls="marketingNav"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="marketingNav">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link text-light" href="#features">Features</a>
            </li>
            <li class="nav-item">
              <a class="nav-link text-light" href="#how-it-works">How it Works</a>
            </li>
            <li class="nav-item">
              <a class="nav-link text-light" href="{{ url_for('marketing.pricing') }}">Pricing</a>
            </li>
            <li class="nav-item">
              <a class="nav-link text-light" href="#docs">Docs</a>
            </li>
            <li class="nav-item">
              <a class="nav-link text-light" href="mailto:hello@donordocs.com">Contact</a>
            </li>
          </ul>
          <div class="d-flex gap-2">
            <a class="btn btn-outline-primary" href="{{ url_for('dashboard') }}" aria-label="Open app dashboard">
              Open App
            </a>
            <a class="btn btn-primary" href="{{ url_for('dashboard') }}" aria-label="Get started with DonorDocs">
              Get Started
            </a>
          </div>
        </div>
      </div>
    </nav>

    <main>
      {% block content %}{% endblock %}
    </main>

    <footer class="marketing-footer">
      <div class="container py-4 d-flex flex-column flex-md-row align-items-center justify-content-between gap-3">
        <span class="text-muted">© {{ "now"|date("Y") }} DonorDocs. All rights reserved.</span>
        <div class="d-flex flex-wrap gap-3">
          <a class="text-muted" href="#privacy">Privacy</a>
          <a class="text-muted" href="#terms">Terms</a>
          <a class="text-muted" href="mailto:hello@donordocs.com">Contact</a>
        </div>
      </div>
    </footer>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
